# This file is for Docker Compose configuration.
# It must be in the project's root directory.

# The full public URL for the frontend Next.js app.
# Used by Next.js to request its own assets (JS, CSS).
NEXT_PUBLIC_APP_URL=http://localhost:3000

# The full public URL for the backend API endpoint.
# Used by the frontend to make API calls (e.g., fetch projects).
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# The base public URL for the backend server.
# Used by the backend to create correct, full URLs for uploaded images.
API_SERVER_URL=http://localhost:3001


# Admin Authentication
ADMIN_SECRET_KEY=your_super_secret_key_here
JWT_SECRET=your_random_jwt_secret_here